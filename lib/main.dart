import 'package:flutter/material.dart';
import 'package:namer_app/pageone.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) => MaterialApp(
        home: FirstPage(),
      );
}
